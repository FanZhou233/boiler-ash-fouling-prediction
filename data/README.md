# Data directory

The original `CF1.mat` file is intentionally excluded from the public portfolio package.

For reproducible public demos, place a desensitized sample file here and update the data path in `data_cleaning.py`, or place `CF1.mat` in the project root if you have permission to publish it.
